{
  "name": "wa_server",
  "version": "2.0.0",
  "description": "WhatsApp Integration Server",
  "scripts": {
    "update": "ncu -u",
    "lt": "lt --port 8000",
    "python:dev": "python3 -m venv ENV && ENV/bin/pip3 install -r requirements.txt && ENV/bin/fastapi dev src/main.py",
    "python:prune": "rm -rf ENV && find . -name *.pyc -delete && find . -name __pycache__ -delete"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openai/codex": "^0.42.0",
    "localtunnel": "^2.0.2",
    "npm-check-updates": "^19.0.0"
  }
}
